The TechZeen 

======================[Date : 11/03/2026    Wednesday]=========================
install nest ->  npm i -g @nestjs/client
create project in nestjs - nest new projectname
#) Folder Structure
Inside the nestjs folder structure there are many files and folder in which src folder have our working folder in which have so many files 
a) app.controller.spec.ts   
   This is for testing purpose file 
b) app.controller.ts 
    This file is based on controller working that take request and give response via controller.
c) app.module.ts 
    This is out root module file that are attached with all services.
d) app.service.ts 
    This file in which we write the our business logic and controller also call this file app.service.ts to take request and give repsponse as according to request
e) main.ts 
    This is the entry file of our nest js app

========================[Date : 12/03/2026   Thursday]===============================
Create controller - nest g controller controllerName (student)
Create Module     - nest g module moduleName (student)
Create Service    - nest g service serviceName (student)
a) Today we complete the CRUD Operation in it without databse operation and also perform the API Method in it.
GET, POST, PUT, PATCH, DELETE

11) ========================[Date : 13/03/2026    Friday]==============================
it create manually not by command
{#} DTO (Data Transfer Object)

a) An object that carries data between layers (like from client to backend).
b) Used to define the shape of incoming request data.
c) Ensures only required data is passed (security + validation).

{#} Interface
it create manually  not by command
a) Interfaces define the structure (type) of an object.
b) Help write clean, structured, type-safe code.
c) Used for both request(DTOs) and response objects.

In which the customer folder is make for DTO implementation

12)=====>>>>>>>>>> CLASS VALIDATOR, CLASS transformer

this is bby default make pipe that use in globally our code in main.ts file check it 
app.useGlobalPipe that term is belong here 

Now we learn and implement the validation/ validator on dto because on compile time it compile but on run time it not 
i.e., /* eslint-disable prettier/prettier */
 export class CreateCustomerDto{
    name: string;
    age: number;
 }

 on run time if we add a another key it processed not give any error beacuse of this we use some explicitly validator/ validation

 Now we install the package , class-validator and class-transformer 
 we use this package in DTo of customer for some decorator and also used globally inside the main.ts file 

13)=========>>>>>>>>> Custom Pipes
create custom pipe   nest g pipe common/pipes/uppercase

a) Pipes are used to transform or validate incoming data.
b) Nest js allows you to create your own Custom Pipes.
c) They can be used for custom validation, data transformation, or business logic filtering.
        ------important-----
d) A Pipe runs before the data hits the route handler (controller method).
e) You can apply pipes at method level, controller level, or globally.
f) Custom Pipes implement the Pipe Transform interface.

==> In myname folder we implement the custom pipe implementation and in common/pipes/uppercase  have the method of custom pipe that make the  name in uppercase.

14) =======>>>>>>>How to Protect Routes using Guards 

a) It means restricting  access to specific  API routes.
b) Only authorized users (like logged-in users or admins) can access them.

        ======Guards
command  ---   nest g guards guards/auth

c) Guards are classes that implement logic to decide whether a request is allowed or not.
d) Thay implement the CanActivate interface and run before the route handler.
e) Mostly used for authentication or  authorization.
        ===== Why we used Guards
f) Top secure private routes.
g) To avoid duplicating checks in every controller.
h) To build role-based access control systems. 

15)  ========> Role based  authorization
    By using guards we make this protect the routes and by using guard we make protect and role based acces the user as it is have admin and user

                Date : 16/03/2026   Monday

16)   =========> Exception Filters in Nest js 
Command -> nest g filter filters/http-exception
Folder -> exception, filters

a) Handle errors and exception in a centralized way.
b) Help in managing app-wide error handling logic cleanly and consistently.
      ------ Where we use this Filters
c) Filters can be applied at method-level, controller-level, or globally (in main.ts).
d) @Catch() decorators  is used  to define  which exception  the filter  will handle.

now make a controller  ExceptionController
nest g controller exception


17)  =======> Middleware 
a) Middleware runs before the request reaches the controller.

            Middleware - UseCases
b) Logging incoming requests
c) Authentication tokens (e.g., checking JWT )
d) Request transformation (e.g., converting strings  to numbers)
e) Blocking or redirecting requests
f) Setting headers.

        Middleware v/s Guard  ---. important
Command ->  nest g middleware middleware/logger

Guard -> a) Before route is accessed (based on auth)
         b) Authorization (role check., access allowed?)
Middleware - > a) Before controller
               b) Common tasks (logging, token decode)



